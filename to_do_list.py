# A command-line app where you can add, view, delete,
# and mark tasks as done — basically a mini task manager that runs in the terminal.
list = []
for i in range():
    list.appennd(input("Enter Your task:"))
    